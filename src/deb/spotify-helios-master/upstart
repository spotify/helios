# helios-master - manages jobs across helios agents

description "helios master"
author "Rohan Singh <rohan@spotify.com>"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

pre-start script
  [ -f /etc/default/helios-master ] && . /etc/default/helios-master

  # don't start unless explicitly enabled in config
  [ -z "$ENABLED" ] && { stop; exit 0; }
end script

script
  /usr/bin/helios-master $HELIOS_MASTER_OPTS
end script
