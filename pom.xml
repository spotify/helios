<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <name>Spotify Helios Parent</name>
  <groupId>com.spotify</groupId>
  <artifactId>helios-parent</artifactId>
  <version>0.0.9-SNAPSHOT</version>
  <packaging>pom</packaging>

  <dependencies>
    <dependency>
      <groupId>com.intellij</groupId>
      <artifactId>annotations</artifactId>
      <version>12.0</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <build.timestamp.format>yyyyMMdd-HHmmss.SSS</build.timestamp.format>
    <timestamp>${build.timestamp}</timestamp>

    <sonar.host.url>https://sonar.spotify.net</sonar.host.url>
    <sonar.jdbc.url>jdbc:postgresql://real.sonar.spotify.net/sonardb</sonar.jdbc.url>
    <sonar.jdbc.username>sonaruser</sonar.jdbc.username>
    <sonar.jdbc.password>Aecausuupaijahshae2re6ie</sonar.jdbc.password>
  </properties>

  <modules>
    <module>helios-client</module>
    <module>helios-tools</module>
    <module>helios-services</module>
    <module>helios-integration-tests</module>
  </modules>

  <profiles>
    <profile>
      <id>default</id>
      <activation>
        <property>
          <name>!debug</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>2.16</version>
            <configuration>
              <enableAssertions>false</enableAssertions>
              <forkCount>2C</forkCount>
              <reuseForks>true</reuseForks>
              <argLine>-Xmx128m</argLine>
              <systemPropertyVariables>
                <heliosLoggingLevel>OFF</heliosLoggingLevel>
                <externalLoggingLevel>OFF</externalLoggingLevel>
                <rootLoggingLevel>OFF</rootLoggingLevel>
              </systemPropertyVariables>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>debug</id>
      <activation>
        <property>
          <name>debug</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <version>2.16</version>
            <configuration>
              <enableAssertions>false</enableAssertions>
              <systemPropertyVariables>
                <heliosLoggingLevel>DEBUG</heliosLoggingLevel>
                <externalLoggingLevel>ERROR</externalLoggingLevel>
                <rootLoggingLevel>ERROR</rootLoggingLevel>
              </systemPropertyVariables>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>local</id>
      <activation>
        <activeByDefault>true</activeByDefault>
        <property>
          <name>!teamcity.version</name>
        </property>
      </activation>
      <properties>
        <version_suffix>.LOCAL</version_suffix>
      </properties>
    </profile>

    <profile>
      <id>teamcity</id>
      <activation>
        <property>
          <name>teamcity.version</name>
        </property>
      </activation>
      <properties>
        <version_suffix>.${build.number}</version_suffix>
      </properties>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>com.soebes.maven.plugins</groupId>
              <artifactId>maven-echo-plugin</artifactId>
              <version>0.1</version>
              <executions>
                <execution>
                  <phase>generate-sources</phase>
                  <goals>
                    <goal>echo</goal>
                  </goals>
                </execution>
              </executions>
              <configuration>
                <echos>
                  <echo>##teamcity[buildNumber '${project.version}~${git.commitsCount}.${timestamp}.${git.commit.id.describe}${version_suffix}']
                  </echo>
                </echos>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
    <profile>
      <id>integration-only</id>
      <build>
        <plugins>
	  <plugin>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
	      <skip>true</skip>
            </configuration>
	  </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>2.5.1</version>
        <configuration>
          <source>1.7</source>
          <target>1.7</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>1.2</version>
        <executions>
          <execution>
            <id>enforce</id>
            <configuration>
              <rules>
                <requireUpperBoundDeps/>
              </rules>
            </configuration>
            <goals>
              <goal>enforce</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>2.4</version>
        <configuration>
          <archive>
            <manifest>
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>pl.project13.maven</groupId>
        <artifactId>git-commit-id-plugin</artifactId>
        <version>2.1.7</version>
        <executions>
          <execution>
            <goals>
              <goal>revision</goal>
            </goals>
            <phase>initialize</phase>
          </execution>
        </executions>
        <configuration>
          <dotGitDirectory>../.git</dotGitDirectory>
          <gitDescribe>
            <abbrev>7</abbrev>
            <always>true</always>
            <forceLongFormat>true</forceLongFormat>
            <dirty>-DIRTY</dirty>
          </gitDescribe>
        </configuration>
      </plugin>
      <plugin>
        <groupId>ru.concerteza.buildnumber</groupId>
        <artifactId>maven-jgit-buildnumber-plugin</artifactId>
        <version>1.2.7</version>
        <executions>
          <execution>
            <id>git-buildnumber</id>
            <goals>
              <goal>extract-buildnumber</goal>
            </goals>
            <phase>initialize</phase>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
